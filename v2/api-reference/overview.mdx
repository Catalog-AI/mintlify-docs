---
title: "Overview"
description: "Complete reference for the Catalog API endpoints"
---

The Catalog API provides endpoints for product search, discovery, and commerce intelligence. All endpoints return JSON responses.

## Base URL

```
https://api.getcatalog.ai
```

## Authentication

All requests require an API key in the `x-api-key` header. See [Authentication](/v2/authentication) for details.

## Endpoints

### Discover

| Endpoint | Method | Description |
|----------|--------|-------------|
| [`/v2/crawl`](/v2/api-reference/endpoints/discover/start-crawl) | POST | Start asynchronous crawling of a vendor website |
| [`/v2/crawl`](/v2/api-reference/endpoints/discover/list-crawl-executions) | GET | List recent crawl executions |
| [`/v2/crawl/{execution_id}`](/v2/api-reference/endpoints/discover/get-crawl-status) | GET | Check the status of a crawl execution |
| [`/v2/crawl/{execution_id}`](/v2/api-reference/endpoints/discover/cancel-crawl-execution) | DELETE | Cancel a running crawl execution |
| [`/v2/vendors`](/v2/api-reference/endpoints/discover/get-vendors) | GET | List all vendors with product counts |
| [`/v2/collections`](/v2/api-reference/endpoints/discover/get-collections) | POST | List collections for a vendor |
| [`/v2/listings`](/v2/api-reference/endpoints/discover/get-products) | POST | List products from a vendor or collection |

### Extract

| Endpoint | Method | Description |
|----------|--------|-------------|
| [`/v2/extract`](/v2/api-reference/endpoints/extract/extract-direct-urls) | POST | Extract products from direct URLs (1-1000 URLs) - Direct URLs mode |
| [`/v2/extract`](/v2/api-reference/endpoints/extract/extract-vendor-based) | POST | Extract products from a vendor domain - Vendor-based mode |
| [`/v2/extract`](/v2/api-reference/endpoints/extract/list-extract-executions) | GET | List recent extraction executions |
| [`/v2/extract/{execution_id}`](/v2/api-reference/endpoints/extract/get-extract-status) | GET | Check status and retrieve results for an extraction job |
| [`/v2/extract/{execution_id}`](/v2/api-reference/endpoints/extract/cancel-extract-execution) | DELETE | Cancel a running extraction execution |

### Search

| Endpoint | Method | Description |
|----------|--------|-------------|
| [`/v2/agentic-search`](/v2/api-reference/endpoints/search/agentic-search) | POST | AI-powered search with customer profile personalization |
| [`/v2/agentic-search-mini`](/v2/api-reference/endpoints/search/agentic-search-mini) | POST | Fast AI search returning up to 10 products |

### Affiliate

| Endpoint | Method | Description |
|----------|--------|-------------|
| [`/v2/affiliate`](/v2/api-reference/endpoints/affiliate/generate-links) | POST | Generate affiliate tracking links |

### Usage

| Endpoint | Method | Description |
|----------|--------|-------------|
| [`/v2/usage`](/v2/api-reference/endpoints/usage/get-usage) | GET | Get API usage statistics |

## Response format

All successful responses follow a consistent structure with a `data` array and `pagination` object:

```json
{
  "data": [...],
  "pagination": {
    "page": 1,
    "page_size": 10,
    "total_items": 100,
    "total_pages": 10,
    "has_next": true,
    "has_prev": false
  },
  "meta": {}
}
```

**Product execution status** returns results in a similar structure:
```json
{
  "execution_id": "...",
  "status": "completed",
  "data": [...],
  "pagination": {
    "page": 1,
    "page_size": 50,
    "total_items": 100,
    "total_pages": 2,
    "has_next": true,
    "has_prev": false
  },
  "meta": {
    "progress": {...},
    "result": {
      "products_requested": 100,
      "products_successful": 98,
      "products_failed": 2
    }
  }
}
```

## Error handling

Errors return appropriate HTTP status codes with structured error details:

```json
{
  "error": {
    "code": "ERROR_CODE",
    "message": "Human-readable error description",
    "request_id": "req_..."
  }
}
```

Additional fields may be included for specific error types. For example, validation errors may include:
```json
{
  "error": {
    "code": "VALIDATION_ERROR",
    "message": "One or more URLs have invalid format",
    "request_id": "req_..."
  },
  "invalid_urls": [
    "Index 0: https://invalid-url - invalid format"
  ]
}
```

All error responses include a `Request-ID` header that matches the `request_id` in the response body.

See [Error Codes](/resources/error-codes) for the complete reference.
